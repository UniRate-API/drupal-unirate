unirate.settings:
  type: config_object
  label: 'UniRate Currency settings'
  mapping:
    api_key:
      type: string
      label: 'API key'
    base_url:
      type: string
      label: 'API base URL'
    timeout:
      type: integer
      label: 'Request timeout (seconds)'
